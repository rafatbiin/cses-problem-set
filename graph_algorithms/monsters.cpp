#include <bits/stdc++.h>
using namespace std ;

#define DEBUG(x) cout << '>' << #x << ':' << x << endl;
#define mem(x,val) memset((x),(val),sizeof(x))
#define all(x) x.begin(),x.end()
#define pb push_back
#define mp make_pair
#define PI acos(-1.0) 
#define N 1111111
#define M 1000000007
#define endl "\n"

const int INF = 1 << 30 ;
typedef long long ll ;
typedef pair < int , int > pii ;

vector<int> g[N] ;
char input[N][N] ;
int dc[N][N], parent[N][N], a[N][N], m[N][N] ;
int dx[]= {1, 0, -1, 0} ;
int dy[] = {0, 1, 0, -1} ;
int dc[] = {'D', 'R', 'U', 'L'} ;
bool visited[N][N] ;

void bfs(int x, int y){

}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n, m ;
    cin >> n >> m ;

    

    return 0 ;
}